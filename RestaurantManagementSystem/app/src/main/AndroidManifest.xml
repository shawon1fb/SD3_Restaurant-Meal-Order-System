<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shawon.restaurantmanagementsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/rlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/rlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SetMeal" />
        <activity android:name=".SetMealActivity" />
        <activity android:name=".CustomeSetMealActivity" />
        <activity android:name=".SetMealOrderList" />
        <activity android:name=".AboutDeveloper"></activity>
    </application>

</manifest>